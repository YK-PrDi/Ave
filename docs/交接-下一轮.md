# 交接：下一轮对话从这里开始

> 顶部这节写于 **2026-09-07**（云端 BGM 曲库 + 署名）。
> 再往下「上一轮做完的四件事」及之后是 2026-08-27 那轮的记录，仍然有效。
> **先读 `CLAUDE.md`（铁律 18 条），再读本文件，
> 碰 BGM / 音乐版权先读 `docs/BGM授权决策.md`（别重新核实授权，都查过了）。**

## 一句话状态（2026-09-08）

**云端 BGM 曲库 312 首已上线、片尾署名已实现并提交、打包版已重打并验通。**
`Ave-分发.zip`（183.4 MB）可发运营，这条线做完了。

## ✅ 重打包已完成（2026-09-08）

打了**两次**：第一次带上了云端曲库，第二次清掉了 Pixabay 内置层（见下）。
最终产物 `Ave-分发.zip` **183.4 MB / 2029 项**，在**仓库根目录**（不是 `dist/`）。

打包版实测（`Ave.exe --no-browser` + `/api/jobs`，不是看界面数字）：

| 验的事 | 结果 |
|---|---|
| 随包凭证带云端配置 | ✅ 7 键含 `BGM_CLOUD_MANIFEST`（旧包 6 键，正缺这个） |
| 打包版真拉到云端库 | ✅ `/api/bgm` 返回 312 首、版本 `2026-09-08`、`note` 空 |
| **`bgm_id` 是云端 id** | ✅ `Local_Forecast` / `Cold_Sober` / `Magic_Scout_-_Farm` |
| 署名烧进画面 | ✅ 两条各自正确，截图 `.probe/credit_00{1,2}.png` |
| 缓存命中不重下 | ✅ 渲染前后 mp3 大小全未变、无 `.part` |
| 断网 | ✅ 出片 1/1 不崩，明确报「1 首云端 BGM 下载失败」 |
| 清掉内置层后 | ✅ zip 里 **0 个 mp3**、`_internal/bgm` 不存在、`builtin: 0` |

⚠️ 随包凭证是**明文**，拿到 dist 文件夹的人能提取出 token。仅限内部分发。

### ⚠️ 实测纠正本文档原先两处说法

1. **断网不回落内置 BGM**，而是**那条片子无 BGM**。`pick_bgm()` 把三层
   （内置 + 自定义 + 云端）合成一个池**统一随机**，抽中的云端曲下载失败就是
   「这条没 BGM」，不会改抽本地。它**如实报错**（符合铁律 9），
   但原先写的「断网回落到内置 BGM」与实现不符。
2. **`SOURCE-OFFER.md` 的 `<待填>` 早已补齐**（release `v0.1.0`，两个资产
   已上传、SHA256 匿名下载比对到端），`准备素材.py --check` 三项全打勾。
   是本文档没跟着更新 —— 别再按「待填」去改它。

### 🔴 Pixabay 内置层已移出，别再放回去

原先仓库 `bgm/` 里那 7 首 Pixabay 曲子会被 `Ave.spec:64` 自动打进包，
而 `docs/BGM授权决策.md:158` 判定**分发 BGM 库不合规**（Standalone 条款）。
已移到 `D:\code\bgm-pixabay-已移出\`（没删），仓库 `bgm/` 现在是空的，
spec 走 else 分支只打印一行 `no builtin BGM`，**不报错**。

顺带修掉一个静默 bug：内置层和自定义层曾是**同一批 7 个文件**（同名同字节），
`config.bgm_layers()` 按 `realpath` 去重抓不到（两个路径确实不同），
于是那 7 首在候选池里各占两份、**抽中概率翻倍**。清空内置层后重叠消失
（`bgm_count` 14 → 7）。

⚠️ **要随包发曲子只能发允许再分发的**（CC-BY / CC0），
Pixabay 的只能走清单存官方直链、客户端自己拉。

## ✅ 12 首待核实已闭环（2026-09-08）

用户听完确认，已移回主目录 → 曲库 **300 → 312 首**。

**⚠️ 关键一步别漏：先合并边车，再生成清单。** 那 12 首的元数据原先在
单独的 `待核实\来源-待核实.json` 里，主边车 `来源.json` 只有 300 条。
而 `生成清单.py:179` 按文件名查边车，**查不到就用命令行 `--license` 默认值填** ——
不合并的话这 12 首会被贴上 `Pixabay Content License`（错误授权）。
脚本会报警不会静默，但结果是错的。

已做：边车 300 → 312 条（原件备份 `来源.json.bak-2026-09-08`），
`_核对不过` 标记已清（已听过确认，留着误导后来人）。
重生成清单**没有**出现「有 N 首不在边车里」警告 = 312 首授权全有凭据。

**上传顺序有硬约束：先传 mp3，最后传 manifest。** 反了的话客户端会拿到
指向不存在文件的清单，抽中就 404（不崩、如实报错，但那条片子没 BGM）。

线上验完（只读请求）：312 首 / 版本 `2026-09-08` / id 与本地逐一致 /
12 个新 mp3 全部 200 且大小逐首对得上 / 端到端下载 `Future_Cha_Cha`
**sha256 校验通过**（证明上传未损坏）/ 署名文本正确生成。

**已在跑的客户端不用重发包** —— 清单 TTL 10 分钟，下次拉自动看到 312 首。

## 补 BGM 库怎么做（2026-09-07 已跑通这条路）

现在 312 首。要加更多，**同库追加边际风险≈0**（授权已验），流程：

```bash
# 1. 看筛选结果（不下载）。改筛法就动脚本里 KEEP / DROP / DROP_GENRES 常量
python 抓取incompetech.py --dry-run

# 2. 下载（已存在的自动跳过，可反复跑）
python 抓取incompetech.py -o "D:\bgm-incompetech"

# 3. 上传新增的 mp3 到 COS 的 bgm/ 目录，然后重生成清单
python 生成清单.py "D:\bgm-incompetech" --meta "D:\bgm-incompetech\来源.json" ^
  --host own --license CC-BY-4.0 ^
  --base https://graduation-project-1416091844.cos.ap-guangzhou.myqcloud.com/bgm/

# 4. 把新 manifest.json 覆盖上传
```

**想放宽筛法多要曲子**：`抓取incompetech.py` 里 1442 首中当前筛出 312。
放宽的可动项，按「风险从低到高」：
① 时长闸 `MIN_SEC/MAX_SEC`（现 60~420 秒）—— 最安全，只是取舍长度；
② `DROP_GENRES` 里放回 Silent Film Score / Polka（28+12 首）—— 风格对但年代感强；
③ 把 `Grooving` 单独作为入选依据 —— **不建议**，实测会放进华尔兹和圣诞颂歌。

**换别的曲库**（incompetech 挖完了）：第六节列了 FAQ 自荐的 8 个源，
授权要逐一核实，**别假设「免费站 = 可商用」**（第四节陷阱 1 的教训：
Free Music Archive 上那首是 CC BY-**NC**，禁商用）。
新库要单独跑一份边车，**不要和 incompetech 共用** —— `生成清单.py` 现在会
把不在边车里的曲子明确报出来，共用会让授权字段套用错的默认值。

⚠️ **Pixabay 来源的曲子不准进这个桶**。它的 Content License 禁止原样再分发，
清单里 `host` 字段就是为防手滑而设的（`own` / `pixabay`）。理由见第六节。

## ✅ 19 个依赖漏洞 —— 早就查完了（2026-08-28）

**本节原先写着「我没查过，别假设我查过」，那是过期的。**
`docs/进度与待办.md`「依赖安全（2026-08-28）」记着已查完：19 条
（13 HIGH + 6 MODERATE）**全部出自一个包 `pillow==12.0.0`**，
已升到 `12.3.0`（`requirements.txt:9` 核实过就是这个版本），19 条全清。

暴露面分析也做了：Pillow 只用在 `subtitle.py`（渲字幕 PNG）和
`准备素材.py`（校验下载的字体），都是**写出**不是解析不可信输入；
19 条里 PSD/PDF/JPEG2000 等解码器全部用不到。所以当初是**清告警**，
不是修实际可利用漏洞。

⚠️ 下次再看到 Dependabot 报数，查法留档在那一节：`npm audit` 要
**指定官方源**（`--registry=https://registry.npmjs.org`，本机 npmmirror
没实现 audit 接口会 404）；无 `gh` CLI 时走 **OSV.dev 批量查**
（同一个 GitHub Advisory Database）。且要分清「`requirements` 里有」
和「真打进包里」—— `Ave.spec` 的 `excludes` 排掉了 torch 等一大堆，
判据是看 `dist/Ave/_internal/` 里有没有那个包。

## 🟡 方舟账号欠费（不阻塞，但会静默降级）

点「测试视觉接口」返回 403 `AccountOverdueError`。
**影响**：AI 补口播只能吃 `CopyStore` 缓存，生成**新**文案会失败
（`build_one()` catch 住回落静音占位，不崩，但那段没口播）。
全量重渲实测：两个无口播片段都取到了缓存，**这一批成品没受影响**。
换素材才会暴露。**动作**：去方舟控制台充值，别改 `vision.py`。

## 上一轮做完的四件事

### 1. BGM 音量定 3%（用户实听，已闭环）

用户听完说「背景音乐可以再降低些，有点感觉影响口播」。渲 0/3/5/8% 四条
试听，**用户定 3%**（≈-30dB）。原来是 -18dB（≈12.6%）。

- `render.py` 的 `BGM_GAIN_DB` 改成百分比制 `bgm_volume`
- 全链路：config → render → pipeline → server → 前端滑块（0~30%）
- CLI 加 `--bgm-volume`，日志会打「BGM 音量: 3.0%」
- 0 是合法值 = 不混 BGM，`render()` 把 bgm 置 None 省一路输入
- 默认值改动要同步三处：`config.BGM_VOLUME`、
  `render.BGM_VOLUME_DEFAULT`、`App.vue` 首屏占位

两个坑进了铁律 15：`amix` 的 `normalize=0` 不能去；判空不准用 `or`（0 会被吞）。

### 2. 全量重渲 + 导出（39/39，用户已听完通过）

**关键教训**：定完 3% 时桌面上那批成品**没有一条是 3% 渲的** ——
37 条是 -18dB 那批、2 条是我验证时跑的 5%。改了默认值不等于成品跟着变，
**BGM 烘在音轨里，必须重渲**（不像倍速有导出通道免重渲）。

重渲 39/39 用 411s，导出 39/39 用 39s。抽验三条时长比都是 1.197（≈1.2）。

### 3. GPL 源码归档上传 release v0.1.0

`https://github.com/YK-PrDi/Ave/releases/tag/v0.1.0`，两个资产已传。
**验证到端**：匿名下载回来，SHA256 与 `SOURCE-OFFER.md` 钉死的值一致、
且与本机 `dist-src/` 逐字节相同（`cmp` 通过）。

`准备素材.py` 的检查从硬编码改成**实查 API** —— 原来写死「尚未上传」，
传完还在喊没传；但反过来写死「已上传」只是换个方向骗人（资产被删就静默）。
现在去问 GitHub、还比文件大小。三个分支都验过：真 tag 打勾、假 tag 报
404、缺资产报红。

**GPL 义务生效**：自 2026-08-27 起**至少三年**别删那两个资产、
别把 release 改 draft 或撤下。

### 4. 凭证打进 exe（🔴 仅限内部分发）

用户 2026-08-27 定：内部使用，免得每台机器手配。

🔴 **明文，拿到 `dist\Ave` 文件夹的人都能提取出 token**，而这几个凭证带计费。
PyInstaller 的 datas 不加密，改个后缀就能读。**拷给谁就等于把 token 给谁。**
对外发布前必须删掉 `Ave.spec` 那段 + `_bundled_credentials.json`。

- `config.py` **逐键合并**两处凭证：用户目录优先、随包兜底。
  逐键而非整表二选一 —— 各公司放自己的 json 就能盖掉随包那份，不用重新打包
- `打包.bat` 第 4 步拷凭证，PyInstaller 跑完立刻 `del`
- `.gitignore` 补了 `credentials*.json`（**原来一条都没有**，是真口子）

两条安全底线都实测过：
1. 版本库拦得住 —— `credentials.json` 和 `_bundled_credentials.json` 都被拦
2. 凭证落包根**不落 `web/`**（那目录被 StaticFiles 对外挂）——
   实测四条 URL 全 404

**关键验证**：临时移开用户凭证后起 exe，health 报 `tts_backend=volcano`，
证明读的是随包那份。测完已还原。

- `render.py` 的 `BGM_GAIN_DB` 改成 `config.BGM_VOLUME` 百分比制 ——
  运营看得懂「5%」，看不懂「-26 分贝」
- 全链路加 `bgm_volume`：`config` → `render.build_filter/render` →
  `pipeline.build_one/run` → `server.JobReq/health` → 前端滑块
- CLI 加 `--bgm-volume`，`start` 事件里带上，日志会打「BGM 音量: 5.0%」
- 前端「参数」卡片第 4 格：滑块 0~30%，实时显示数值 + 分档提示
  （0 = 不加背景音乐 / ≥12 = 偏大，可能压住口播 / 其余 = 越小越不抢口播）
- **0 是合法值**：不混 BGM，`render()` 把 `bgm` 置 None 省一路输入和一次混音

踩到的两个点已写进 CLAUDE.md 铁律 15：`amix` 的 `normalize=0` 不能去，
判空不准用 `or`（0 会被吞成默认值）。

### 附：exe 四项实测（2026-08-27 首次打包时做的）

`Ave.spec` 那个「不用改」的推断是对的，已实测。

| 验的 | 结果 |
|---|---|
| exe 起来、health 正常 | ✅ ffmpeg/font/model 全 True |
| **视觉接口** | ✅ `ave.vision` import 正常、真发出请求 → 但账号欠费 403 |
| **导出按钮** | ✅ `POST /api/export` 可用，`respeed()` 正常 |
| BGM「添加音乐」 | ✅ `Ave.exe --pick-files` 自举 OK |
| 界面滑块 | ✅ 打包版界面渲得出，三档提示文案都对 |

### 附：BGM 音量怎么验（有坑）

**整条成品的 mean_volume 分不出 0/3/5%** —— 都是 -22.7 dB 左右。
原因：BGM 比口播低三十分贝，对全片均值几乎无贡献；且每次渲染 TTS
重新合成，各条时长差到 0.7 秒，逐窗口对比也对不齐。

验缩放要用**受控测试** —— 同一段 BGM 只过 `volume` 滤镜，不混配音：

| 音量 | 实测 | 与上一档差 | 理论 |
|---|---|---|---|
| 3% | -46.0 dB | — | — |
| 5% | -41.6 dB | +4.40 | +4.44 |
| 8% | -37.5 dB | +4.10 | +4.08 |
| 15% | -32.0 dB | +5.50 | +5.46 |
| 30% | -26.0 dB | +6.00 | +6.02 |

每档与理论差 <0.05 dB。0% 那条实测 -95.6 dB（真数字静音）。
**但选哪档只能靠用户的耳朵** —— 客观指标在这里没有裁决权。

## 其余仍未确认的

- 🟡 **AI 文案自然度没经用户单独确认** —— 39 条整体通过了，
  但没专门问过「那两段 AI 补的听不听得出来」
- 🟡 **组合基线「开去重」那项测不了** —— 拿聚类结果当判重基准是循环论证。
  已在文档如实说明，别为了好看编个新数字
- 🟡 `doubao-seed-2-0-lite` 之外三个模型没做端到端出片对比（欠费中，跑不了）
- 🟡 ASR 纠正表只扫了同音词模式，可能有漏。全文在 `.probe/asr_all.txt`
- 🟡 **密钥隔离（每公司一份互不可见）没做** —— 等羽刃接入方案定了再设计。
  注意现在凭证是**随包明文**，隔离需求会和这个设计冲突，届时要一起改


## 环境事实（省得重查）

| 项 | 值 |
|---|---|
| 素材 | `D:\Download\分镜\1~7`，钩子 13 / 卖点 34 / 结尾 8 → 39 条 |
| 输出 | `C:\Users\20739\Desktop\VEDIO抖音`（导出在 `导出_1.2x/` 子目录）|
| 凭证 | `%LOCALAPPDATA%\Ave\credentials.json`（TTS 4 项 + `ARK_API_KEY`）|
| 视觉模型 | `doubao-seed-2-0-lite-260428`（**账号欠费中**）|
| exe | `dist\Ave\Ave.exe`，端口 8756 |
| BGM | 内置层 0 首、自定义层 7 首（`%LOCALAPPDATA%\Ave\bgm`）|

**用户方舟账号已开通的**（实探过，别再猜）：
`2-0-lite-260428` / `2-1-turbo-260628` / `2-0-pro-260215` / `2-1-pro-260628`。
**没开通**：`2-0-mini`、`evolving`（报 `ModelNotOpen`）。

三种错误含义不同：`ModelNotOpen` = ID 对但没开通；
`InvalidEndpointOrModel.NotFound` = ID 本身无效；
`AccountOverdueError` = ID 和权限都对，**账号欠费**。

## 常用命令

```bash
# 出片回归（改完 Python 必跑）
python -m ave.pipeline --limit 2 --seed 7

# 试不同 BGM 音量（不用改代码）
python -m ave.pipeline --limit 1 --seed 7 --bgm-volume 3

# 量成品音量（验 BGM 音量真的变了，别只看日志打印）
./ffmpeg.exe -hide_banner -i "成品.mp4" -af volumedetect -f null - 2>&1 | grep mean_volume

# 验云端曲库通不通（拉清单 + 真下一首）
python -c "from ave import bgm_cloud; t,n=bgm_cloud.fetch_manifest(force=True); print(len(t),'首',n or 'OK'); print(bgm_cloud.ensure_local(t[0]))"

# 验署名对不对：必须渲两条抽到不同 BGM 的，比对 bgm_id 与片尾文字
python -m ave.pipeline --source "D:/Download/分镜/1" --limit 2 --seed 7
# 然后抽末尾前 1 秒那帧看署名（署名窗口是最后 2 秒）
./ffmpeg.exe -y -ss 35 -i "成品.mp4" -frames:v 1 .probe/credit.png

# 全量（9 分钟）
python -m ave.pipeline --seed 7

# 前端类型检查（改完前端必跑）
cd frontend && npx vue-tsc -b --noEmit

# 打包（别用 打包.bat 管道跑，末尾 pause 会挂住；分五步跑）
python 准备素材.py --check
cd frontend && npm run build && cd ..
rm -rf web && cp -r frontend/dist web && cp -r licenses web/licenses
cp "$LOCALAPPDATA/Ave/credentials.json" _bundled_credentials.json   # 内部分发才要
python -m PyInstaller --noconfirm --clean Ave.spec
rm -f _bundled_credentials.json    # ⚠ 手动跑必须自己删，仓库目录别留明文

# 起打包版
cd dist/Ave && ./Ave.exe

# curl 带中文文件名：body 必须写成 UTF-8 文件再 --data-binary，
# 直接 -d '{"names":["混剪_001.mp4"]}' 会被 shell 编码搞坏，报 parsing body
```

## 给下一轮的提醒

1. **用户的耳朵比我的指标准** —— 语速、BGM 音量两轮都是客观指标全过、
   用户一听就发现问题。别拿「音量测过了」当成品可用的证据。
2. **别信「我判断不用改」这类推断** —— `Ave.spec` 这次推对了，但上一轮
   在 `pipeline.py` 把两个分支条件写反过。推断要标明是推断，然后去实测。
3. **验证脚本别重算 `build_theme_map()` 定位片段** —— 读 `.ave-manifest.json`（铁律 4）。
4. **`打包.bat` 有 `pause`**，管道里跑会静默挂住，只看到 cmd 横幅。
