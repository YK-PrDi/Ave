# 版本锁死 —— 给别人的电脑装时避免新版本行为变化。
# 安装：python -m pip install -r requirements.txt
# 国内网络慢就加镜像：-i https://pypi.tuna.tsinghua.edu.cn/simple

# 语音识别（口播转字幕）
faster-whisper==1.2.1

# 字幕渲染成透明 PNG
pillow==12.0.0

# 本地 HTTP 服务，给 Vue3 界面调用
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.12.5

# 繁简转换（可选）。不装则用内置的常见字映射兜底，
# 覆盖实测出现过的繁体字，够用但不全。
opencc-python-reimplemented==0.1.7
